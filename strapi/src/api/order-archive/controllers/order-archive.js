'use strict';

/**
 * Internal-only content-type. Nessuna rotta REST esposta (vedi routes/).
 * Snapshot immutabili di ordini chiusi: scritti da
 * `services/order-close-finalizer/index.js` via `strapi.db.query`.
 */
module.exports = {};
