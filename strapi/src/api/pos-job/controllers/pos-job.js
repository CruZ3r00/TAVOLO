'use strict';

/**
 * Pos-job non è esposto via REST pubblica. I device lo accedono tramite
 * gli endpoint /api/pos-devices/me/jobs (auth X-Device-Token).
 * Lasciamo un controller placeholder per non rompere Strapi core.
 */

module.exports = {};
