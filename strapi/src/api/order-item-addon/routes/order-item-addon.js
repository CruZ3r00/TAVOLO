'use strict';

/**
 * order-item-addon routes
 *
 * Tutte le mutazioni passano dal controller order (creazione contestuale a un
 * OrderItem). Niente route pubbliche.
 */

module.exports = {
  routes: [],
};
